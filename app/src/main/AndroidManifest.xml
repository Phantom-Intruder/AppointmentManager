<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="coursework.com.appointmentmanagement">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme.Base">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddAppointmentActivity"
            android:theme="@style/MyMaterialTheme.Base"/>
        <activity android:name=".DeleteChoiceActivity"
            android:theme="@style/MyMaterialTheme.Base"/>
        <activity android:name=".DeleteEntryActivity"
            android:theme="@style/MyMaterialTheme.Base"/>
        <activity android:name=".ViewChoiceActivity"
            android:theme="@style/MyMaterialTheme.Base"/>
        <activity android:name=".MoveAppointmentActivity"
            android:theme="@style/MyMaterialTheme.Base"/>
        <activity
            android:name=".ThesaurusActivity"
            android:label="Thesaurus"
            android:theme="@style/AppThemeBorderless" />
        <activity
            android:name=".ThesaurusReplaceActivity"
            android:label="Thesaurus"
            android:theme="@style/AppThemeBorderless" />
        <activity android:name=".SearchActivity" />
    </application>

</manifest>